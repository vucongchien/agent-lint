{
  "name": "agent-lint",
  "version": "0.1.0",
  "description": "CLI tool for linting and auto-syncing i18n & design tokens for React/Next.js with AI Agent support",
  "type": "module",
  "bin": {
    "agent-lint": "./dist/bin.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/bin.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts src/bin.ts --format esm --dts --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@chien_swe/core": "workspace:*",
    "@chien_swe/observatory": "workspace:*",
    "commander": "^13.1.0",
    "ora": "^8.2.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {}
}
